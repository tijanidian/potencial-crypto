<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cripto.potencial.tjdian">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Potencialcrypto">

        <activity android:exported="false"
            android:name=".features.login.presentation.LoginActivity"
            >
            <intent-filter>
                <action android:name=".features.login.presentation.LoginActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>


        <activity
            android:name=".features.cryptocurrency.presentation.DetailCryptoCurrencyActivity"
            android:exported="false" />
        <activity
            android:name=".features.main.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>